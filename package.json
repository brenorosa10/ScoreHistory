{
  "name": "scorehistory",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "build:frontend": "npm --prefix frontend run build",
    "dev:backend": "dotnet run --project backend/ScoreHistory.Api.csproj --launch-profile http",
    "build:backend": "dotnet build ScoreHistory.slnx"
  }
}
